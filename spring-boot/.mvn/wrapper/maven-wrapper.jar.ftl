# Placeholder for maven-wrapper.jar
# Binary jar is normally checked into the project. If you prefer not to store the jar in the template,
# the generator can either: (A) copy a real maven-wrapper.jar into the generated project, or
# (B) run `mvn -N io.takari:maven:wrapper` on user's machine to create the wrapper files.
# Leave this file as-is or replace with the real jar bytes during generation.
